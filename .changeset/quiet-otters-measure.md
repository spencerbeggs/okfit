---
"@okfit/core": minor
---

## Features

Exports `CONFIG_SCHEMA_VERSION` (`"1.0"`), the version of the config JSON Schema `okfitConfigDocumentFields` describes. `@okfit/engine`'s published schema host derives its `versions` from this constant instead of restating it, so the two can never disagree.
