# Incident

* [The 0.3.0 release died on an unprovided service](release-died-on-missing-layer.md) - The CLI shipped with a layer built but never merged into the runtime, so every command failed at startup for one day.
