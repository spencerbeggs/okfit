---
"@okfit/cli": minor
---

## Features

* `okfit context`'s `human` output now renders each type's constraints under its bullet: required keys, whether `verified` is required, and its declared fields with their enum values or `path` kind — so the same information a lint error would otherwise be the first place to reveal is visible up front.
