---
type: Consumer
title: effect-playground
description: A scratch repository that consumes every published package and exercises the CLI end to end.
repository: spencerbeggs/effect-playground
tags: [dx]
---

# effect-playground

Exercises the `validate` and `sync` commands against a hand-written bundle. The edge sits at the CLI; it never imports `@okfit/core` directly. Open question: whether it should also exercise the MCP server.
